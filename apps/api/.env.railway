# Railway demo env (committed on purpose)
# NOTE: This is intentionally NOT production-safe. Rotate secrets for any real deployment.
#
# DATABASE_URL and REDIS_URL are expected to be injected by Railway when you attach
# Postgres/Redis services to the API service. You can also set them explicitly here.

NODE_ENV=production
API_HOST=0.0.0.0

# LLM Provider (demo: no external credentials)
LLM_PROVIDER=mock

# x402 Payment (Cronos Testnet)
X402_FACILITATOR_BASE_URL=https://facilitator.x402.org
X402_NETWORK=cronos-testnet
X402_ASSET_CONTRACT=0xc01efAaF7C5C61bEbFAeb358E1161b537b8bC0e0
X402_ASSET_DECIMALS=6
X402_CHAIN_ID=338
X402_EIP712_NAME=x402
X402_EIP712_VERSION=1

# Demo default: mock payments (set to false if you want on-chain settlement)
X402_MOCK_MODE=true

# Security (demo-only)
JWT_SECRET=insecure-demo-jwt-secret-change-me-please-32chars
# 32 bytes base64 (valid AES-256-GCM key). For real deployments generate with: openssl rand -base64 32
AGENT_WALLET_ENCRYPTION_KEY=MDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDA=

# Optional: auto-promote one wallet to admin on first login
# INITIAL_ADMIN_ADDRESS=0xYourAdminWalletAddress
