{
  "name": "soulforge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"pnpm --filter web dev\" \"pnpm --filter api dev\"",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "seed": "pnpm --filter api seed:local",
    "demo": "pnpm demo:infra && pnpm demo:wait && pnpm demo:seed && pnpm demo:dev",
    "demo:onchain": "pnpm demo:infra && pnpm demo:wait && pnpm demo:seed:onchain && pnpm demo:dev:onchain",
    "demo:infra": "cd infra && docker compose up -d",
    "demo:wait": "echo 'Waiting for database...' && sleep 3",
    "demo:seed": "DEMO_MODE=true X402_MOCK_MODE=true LLM_PROVIDER=mock pnpm --filter api seed:local",
    "demo:seed:onchain": "X402_MOCK_MODE=false LLM_PROVIDER=mock pnpm --filter api seed:local",
    "demo:dev": "DEMO_MODE=true X402_MOCK_MODE=true LLM_PROVIDER=mock NEXT_PUBLIC_DEMO_MODE=true concurrently -n web,api -c cyan,magenta \"NEXT_PUBLIC_DEMO_MODE=true pnpm --filter web dev\" \"DEMO_MODE=true X402_MOCK_MODE=true LLM_PROVIDER=mock pnpm --filter api dev\"",
    "demo:dev:onchain": "X402_MOCK_MODE=false LLM_PROVIDER=mock concurrently -n web,api -c cyan,magenta \"pnpm --filter web dev\" \"X402_MOCK_MODE=false LLM_PROVIDER=mock pnpm --filter api dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.15.1"
}
